# Runtime dependencies.
# mcp is capped below 2.0 by default because that is the generation this has
# been tested against end to end; server.py also supports 2.x via an import
# shim, so you can lift the cap once you have re-run the test suite.
mcp>=1.9.0,<2.0.0
httpx>=0.27.0,<1.0.0
